# Быстрый старт с Supabase Storage

## Шаг 1: Создайте проект на Supabase (5 минут)

1. Перейдите на https://supabase.com
2. Нажмите "Start your project" и зарегистрируйтесь
3. Создайте новый проект:
   - Выберите организацию или создайте новую
   - Введите название проекта
   - Выберите регион (ближайший к вам)
   - Введите пароль для базы данных
   - Нажмите "Create new project"
4. Подождите 2-3 минуты пока проект создается

## Шаг 2: Получите ключи доступа

1. В панели проекта найдите раздел **Settings** (шестеренка внизу слева)
2. Перейдите в **API**
3. Скопируйте следующие значения:
   - **Project URL** - это ваш `SUPABASE_URL`
   - **service_role** ключ (секретный) - это ваш `SUPABASE_KEY`
     ⚠️ **ВАЖНО**: Используйте именно `service_role` ключ, а не `anon` ключ!

## Шаг 3: Создайте бакет для хранения кадров

1. В левом меню найдите **Storage**
2. Нажмите **New bucket**
3. Настройки:
   - **Name**: `frames` (или другое имя)
   - **Public bucket**: ✅ Включите, если хотите публичный доступ к файлам
4. Нажмите **Create bucket**

## Шаг 4: Настройте приложение

Создайте файл `.env` в папке `Редактор`:

```env
BUCKET_ENABLED=true
BUCKET_TYPE=supabase
SUPABASE_URL=https://ваш-проект-id.supabase.co
SUPABASE_KEY=ваш-service-role-ключ
SUPABASE_BUCKET=frames
```

Замените:
- `ваш-проект-id` - на ID вашего проекта из URL
- `ваш-service-role-ключ` - на скопированный service_role ключ

## Шаг 5: Установите зависимости и запустите

```bash
cd Редактор
pip install supabase
python app.py
```

Или используйте готовый скрипт:
```bash
install_and_run.bat
```

## Шаг 6: Проверьте работу

1. Откройте http://localhost:5000
2. Перейдите на вкладку **"Извлечение кадров"**
3. Загрузите видеофайл
4. **Отметьте** ✅ "Сохранить в бакет (bucket storage)"
5. Нажмите **"Извлечь кадры"**
6. После обработки проверьте в Supabase:
   - Перейдите в **Storage** → ваш бакет
   - Убедитесь, что кадры появились

## Готово! 🎉

Теперь все извлеченные кадры будут автоматически сохраняться в Supabase Storage, и вы получите прямые ссылки на них.

## Troubleshooting

### Кадры не загружаются

1. **Проверьте ключи:**
   - Убедитесь, что используется `service_role` ключ, а не `anon`
   - Проверьте правильность URL проекта

2. **Проверьте бакет:**
   - Убедитесь, что бакет существует
   - Название бакета в `.env` должно совпадать с созданным бакетом

3. **Проверьте логи:**
   - Посмотрите вывод в консоли при запуске приложения
   - Там будут указаны конкретные ошибки

### Нужна помощь?

Полная документация в [SUPABASE_SETUP.md](SUPABASE_SETUP.md)


