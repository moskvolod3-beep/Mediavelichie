# Пример файла переменных окружения для продакшена
# Скопируйте этот файл в .env и заполните реальными значениями

# ============================================
# Локальный Supabase (PostgreSQL + Studio)
# ============================================
# PostgreSQL пароль для Supabase (если используете локальный Supabase)
POSTGRES_PASSWORD=your-super-secret-postgres-password-change-me

# JWT секрет для Supabase (минимум 32 символа, если используете локальный Supabase)
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long-change-me

# Настройки Supabase Studio (опционально)
STUDIO_DEFAULT_ORGANIZATION=default
STUDIO_DEFAULT_PROJECT=default

# ============================================
# Supabase для продакшена (ОБЯЗАТЕЛЬНО заполните!)
# ============================================
# Если используете облачный Supabase (рекомендуется):
# 1. Создайте проект на https://supabase.com
# 2. Перейдите в Settings → API
# 3. Скопируйте Project URL и anon public key
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key-here

# Service Role Key для Editor (Flask сервер)
# Используется для backend операций (загрузка файлов в Storage)
# Получите из Settings → API → service_role key
# ⚠️ НЕ используйте этот ключ в frontend коде!
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Storage bucket для портфолио
SUPABASE_BUCKET=portfolio

# Если используете локальный Supabase (не рекомендуется для продакшена):
# Оставьте значения по умолчанию или используйте:
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
