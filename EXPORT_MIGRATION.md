# Инструкция по экспорту и развертыванию миграций Supabase

## 📋 Обзор

Это руководство описывает процесс экспорта полной схемы базы данных из локального Supabase и её развертывания на удаленном сервере.

## 🔧 Экспорт схемы из локального Supabase

### Вариант 1: Использование bash скрипта (Linux/macOS/Git Bash)

```bash
# Базовое использование (стандартные параметры)
./export-supabase-schema.sh

# С указанием параметров подключения
DB_HOST=localhost \
DB_PORT=5432 \
DB_USER=postgres \
DB_PASSWORD=your_password \
./export-supabase-schema.sh
```

### Вариант 2: Использование PowerShell скрипта (Windows)

```powershell
# Базовое использование (стандартные параметры)
.\export-supabase-schema.ps1

# С указанием параметров подключения
.\export-supabase-schema.ps1 `
    -DbHost localhost `
    -DbPort 5432 `
    -DbUser postgres `
    -DbPassword "your_password"
```

### Вариант 3: Ручной экспорт через pg_dump

Если скрипты не работают, вы можете экспортировать схему вручную:

```bash
# Установите переменную окружения с паролем
export PGPASSWORD=your_password

# Экспорт схемы
pg_dump \
    -h localhost \
    -p 5432 \
    -U postgres \
    -d postgres \
    --schema-only \
    --no-owner \
    --no-privileges \
    --clean \
    --if-exists \
    > backend/supabase/migrations/YYYYMMDDHHMMSS_full_schema_export.sql
```

### Определение параметров подключения

Если вы используете локальный Supabase через Docker:

1. **Проверьте порт PostgreSQL:**
   ```bash
   docker ps | grep supabase
   # Ищите строку вида: 0.0.0.0:5432->5432/tcp
   ```

2. **Проверьте пароль:**
   ```bash
   # Проверьте .env файл или docker-compose.yml
   grep POSTGRES_PASSWORD .env
   ```

3. **Если используете Supabase CLI:**
   ```bash
   # Порт обычно 54322 для PostgreSQL
   supabase status
   ```

### Что экспортируется

Скрипт экспортирует:
- ✅ Все таблицы и их структуру
- ✅ Индексы
- ✅ Триггеры
- ✅ Функции и процедуры
- ✅ Row Level Security (RLS) политики
- ✅ Последовательности (sequences)
- ✅ Пользовательские типы данных
- ✅ Расширения PostgreSQL

**Важно:** Данные (строки таблиц) НЕ экспортируются, только схема.

## 📤 Коммит миграции

После успешного экспорта закоммитьте файл миграции:

```bash
# Добавьте файл миграции
git add backend/supabase/migrations/YYYYMMDDHHMMSS_full_schema_export.sql

# Закоммитьте
git commit -m "Add full schema migration from local Supabase"

# Отправьте на сервер
git push origin main
```

## 🚀 Развертывание на удаленном сервере

### Шаг 1: Подключитесь к серверу

```bash
ssh user@your-server-ip
cd /opt/mediavelichia
```

### Шаг 2: Обновите код с репозитория

```bash
git pull origin main
```

### Шаг 3: Примените миграцию

#### Вариант A: Использование скрипта (рекомендуется)

```bash
# Применить последнюю миграцию
./deploy-migration.sh

# Или указать конкретный файл миграции
./deploy-migration.sh backend/supabase/migrations/YYYYMMDDHHMMSS_full_schema_export.sql
```

#### Вариант B: Ручное применение

```bash
# Убедитесь, что контейнер Supabase запущен
docker ps | grep mediavelichie-supabase-db

# Примените миграцию
docker exec -i mediavelichie-supabase-db psql -U postgres < backend/supabase/migrations/YYYYMMDDHHMMSS_full_schema_export.sql
```

### Шаг 4: Проверка результата

После применения миграции проверьте состояние базы данных:

```bash
# Список таблиц
docker exec mediavelichie-supabase-db psql -U postgres -c "\dt"

# Список функций
docker exec mediavelichie-supabase-db psql -U postgres -c "\df"

# Проверка RLS политик
docker exec mediavelichie-supabase-db psql -U postgres -c "\d+ table_name"
```

## ⚠️ Важные замечания

1. **Резервное копирование:** Всегда делайте резервную копию базы данных перед применением миграций на продакшене:
   ```bash
   docker exec mediavelichie-supabase-db pg_dump -U postgres > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **Тестирование:** Сначала протестируйте миграцию на тестовой базе данных или локально.

3. **Проверка содержимого:** Перед применением проверьте содержимое файла миграции:
   ```bash
   cat backend/supabase/migrations/YYYYMMDDHHMMSS_full_schema_export.sql | less
   ```

4. **Откат:** Если что-то пошло не так, восстановите из резервной копии:
   ```bash
   docker exec -i mediavelichie-supabase-db psql -U postgres < backup_YYYYMMDD_HHMMSS.sql
   ```

## 🔍 Устранение проблем

### Ошибка: "pg_dump не найден"

**Решение:** Установите PostgreSQL client tools:
- Ubuntu/Debian: `sudo apt install postgresql-client`
- macOS: `brew install postgresql`
- Windows: Скачайте с https://www.postgresql.org/download/

### Ошибка: "не удалось подключиться к базе данных"

**Возможные причины:**
1. Неправильный порт (проверьте через `docker ps`)
2. Неправильный пароль (проверьте `.env` или `docker-compose.yml`)
3. База данных не запущена (запустите: `docker compose up -d supabase`)

### Ошибка при применении миграции: "объект уже существует"

**Решение:** Это нормально, если миграция использует `CREATE OR REPLACE` или `IF NOT EXISTS`. Если ошибка критична, проверьте логи выше.

## 📚 Дополнительные ресурсы

- [Документация pg_dump](https://www.postgresql.org/docs/current/app-pgdump.html)
- [Документация Supabase Migrations](https://supabase.com/docs/guides/cli/local-development#database-migrations)
- [Инструкции по деплою](./DEPLOY.md)
