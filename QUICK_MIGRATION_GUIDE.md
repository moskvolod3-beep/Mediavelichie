# Быстрое руководство по экспорту и развертыванию миграций

## ✅ Что уже сделано

1. ✅ Полная миграция схемы экспортирована из локального Supabase
2. ✅ Файл миграции создан: `backend/supabase/migrations/20260128133013_full_schema_export.sql`
3. ✅ Скрипты для развертывания созданы и закоммичены

## 🚀 Развертывание на удаленном сервере

### Шаг 1: Обновите код на сервере

```bash
ssh user@your-server-ip
cd /opt/mediavelichia
git pull origin main
```

### Шаг 2: Примените миграцию

```bash
# Сделайте скрипт исполняемым
chmod +x deploy-migration.sh

# Примените миграцию
./deploy-migration.sh
```

Скрипт автоматически:
- Проверит готовность базы данных
- Применит миграцию к контейнеру `mediavelichie-supabase-db`
- Покажет статистику и результаты

### Альтернатива: Ручное применение

```bash
docker exec -i mediavelichie-supabase-db psql -U postgres < backend/supabase/migrations/20260128133013_full_schema_export.sql
```

## 📋 Что содержит миграция

Миграция включает полную схему базы данных:
- ✅ Все таблицы (orders, projects, portfolio, reviews, project_videos)
- ✅ Индексы
- ✅ Триггеры
- ✅ Функции (update_updated_at_column)
- ✅ Row Level Security (RLS) политики
- ✅ Последовательности
- ✅ Расширения PostgreSQL

## ⚠️ Важно

1. **Резервная копия:** Перед применением сделайте резервную копию:
   ```bash
   docker exec mediavelichie-supabase-db pg_dump -U postgres > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **Проверка:** Миграция использует `--clean` и `--if-exists`, поэтому она безопасно удалит существующие объекты перед созданием новых.

3. **Тестирование:** Рекомендуется сначала протестировать на тестовой базе данных.

## 📚 Дополнительная информация

Подробные инструкции см. в файле [EXPORT_MIGRATION.md](./EXPORT_MIGRATION.md)
